<% if @publisher_feed.errors.any? %>
  <div id="error_explanation">
  	<h2><%= pluralize(@publisher_feed.errors.count, "error") %>
  		prohibited this feed from being saved
  	</h2>
  	<p>There were problems with the following fields:</p>
  	  <ul>
  	    <% @publisher_feed.errors.full_messages.each do |msg| %>
  	      <li><%= msg %></li>
  	    <% end %>
  	  </ul>
 </div>
<% end %>
